import styled from 'styled-components';

export const AvailableStyled = styled.p`
  display: inline-flex;
  align-items: baseline;
  gap: 8px;
`;

export const AvailableData = styled.span`
  display: inline-flex;
  align-items: baseline;
  gap: 4px;
`;

export const AvailableValue = styled.span`
  font-weight: 700;

  color: var(--primary);
`;

export const AvailableUnit = styled.span`
  color: var(--primary);
`;
